#ifndef __HYDRA_EXPRESSIONS_BITVEC_STRUCT_HPP
#define __HYDRA_EXPRESSIONS_BITVEC_STRUCT_HPP

#include <climits>
#include <string>

#include "expressions/object.hpp"

namespace expr {


// struct BitVec : public Object {
//   BitVec();
//   std::string to_string(interp::LocalRuntime &r, interp::LexicalScope &s);

//   // the byte data
//   unsigned char *bytes;
//   // get an element
//   Object *get_bit(int idx);
//   // get an element(s)
//   Object *get_as(int idx, CType *type);
// };

} // namespace expr

#endif
